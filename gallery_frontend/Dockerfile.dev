# Imagen de DESARROLLO — el código llega por un volumen bind (docker-compose)
# y Next.js recompila en caliente. No es la imagen de producción.

FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3041

CMD ["npm", "run", "dev"]
