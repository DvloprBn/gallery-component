# Portal de documentación — MkDocs Material + el plugin que embebe Swagger UI.
FROM squidfunk/mkdocs-material:9.5.49

# El plugin `swagger-ui-tag` permite incrustar la referencia OpenAPI en vivo
# desde una etiqueta <swagger-ui> en el Markdown.
RUN pip install --no-cache-dir mkdocs-swagger-ui-tag
